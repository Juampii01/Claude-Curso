import type { Module } from "@/types/curriculum";

/**
 * Curriculum data lives here. Edit this file to add modules, sessions,
 * concepts and exercises. Components must NEVER hardcode curriculum content.
 *
 * Module 1 detailed content lands in Phase 4. Modules 2–5 stay as
 * placeholders until each one is approached for real.
 */
export const modules: Module[] = [];
